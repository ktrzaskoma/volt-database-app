spring:
  datasource:
    username: sa
    password: password
    driver-class-name: org.h2.Driver
    url: 'jdbc:h2:mem:volt-db'
  h2:
    console:
      enabled: true
      path: '/h2-console'
  jpa:
    hibernate:
      ddl-auto: create-drop